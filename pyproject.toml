[tool.uv.workspace]
members = [
    "packages/core",
    "packages/observability",
    "services/auth",
    "services/agent-api",
    "services/orchestrator",
    "services/llm-proxy",
    "services/gateway",
]

[tool.uv.sources]
aai-core = { workspace = true }
aai-observability = { workspace = true }

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "--import-mode=importlib"

[tool.uv]
dev-dependencies = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "pytest-cov>=6.0",
    "httpx>=0.27",
    "mypy>=1.11",
    "ruff>=0.6",
]
